# Gradle build cache — should not be published
android/.gradle/

# Nexus credentials — MUST NOT be published
android/gradle.properties

# Native libs (too large for pub.dev, users must add manually)
android/libs/
